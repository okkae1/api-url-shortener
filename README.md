# Создать пользователя
curl -X POST http://localhost:3000/api/v1/users \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Анна",
    "email": "anna@example.com"
  }'
# Удалить ссылку
curl -X DELETE http://localhost:3000/api/v1/links/asdasd
# Обновить ссылку
curl -X PUT http://localhost:3000/api/v1/links/asdasd \
  -H "Content-Type: application/json" \
  -d '{
    "original_url": "https://new-example.com"
  }'# URL Shortener API

RESTful API для управления сокращёнными ссылками и пользователями.

---

## 🔗 Ссылки

- **Метод:**  
  `GET`  
- **URL:**  
  `/links`  
- **Описание:**  
  Получить список всех ссылок  
- **Ответ:**  
  `200 OK` — Массив объектов `Link`

- **Метод:**  
  `GET`  
- **URL:**  
  `/links/:short_code`  
- **Параметры:**  
  `short_code` (в пути) — короткий код ссылки  
- **Ответы:**  
  `200 OK` — Объект `Link`  
  `404 Not Found` — Ссылка не найдена  

- **Метод:**  
  `POST`  
- **URL:**  
  `/links`  
- **Тело:**  
  Объект ссылки (JSON)  
- **Ответы:**  
  `201 Created` — Созданная ссылка  
  `400 Bad Request` — Неверные данные (например, занятый `short_code` или некорректный URL)

- **Метод:**  
  `PUT`  
- **URL:**  
  `/links/:short_code`  
- **Параметры:**  
  `short_code` (в пути) — код ссылки для обновления  
- **Тело:**  
  Обновлённый `original_url` (JSON)  
- **Ответы:**  
  `200 OK` — Обновлённая ссылка  
  `404 Not Found` — Ссылка не найдена  
  `400 Bad Request` — Некорректный URL  

- **Метод:**  
  `DELETE`  
- **URL:**  
  `/links/:short_code`  
- **Параметры:**  
  `short_code` (в пути) — код ссылки для удаления  
- **Ответы:**  
  `204 No Content` — Успешно удалено  
  `404 Not Found` — Ссылка не найдена  

---

## 👤 Пользователи

- **Метод:**  
  `GET`  
- **URL:**  
  `/users`  
- **Описание:**  
  Получить список всех пользователей  
- **Ответ:**  
  `200 OK` — Массив объектов `User`

- **Метод:**  
  `POST`  
- **URL:**  
  `/users`  
- **Тело:**  
  Объект пользователя (JSON)  
- **Ответы:**  
  `201 Created` — Созданный пользователь  
  `400 Bad Request` — Пользователь с таким email уже существует  

---

## 📦 Примеры запросов

```bash
# Получить ссылку по short_code
curl -X GET http://localhost:3000/api/v1/links/asdasd# api-url-shortener
